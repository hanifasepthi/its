{
  "name": "its-maps-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && node scripts/qa-map-data-public.mjs && wrangler deploy --dry-run --outdir .wrangler-build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.112.0"
  }
}
